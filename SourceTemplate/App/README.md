# App/
Entry point y configuración global. Mové acá el `<TuApp>App.swift` (@main)
que generó Xcode. También: configuración del ModelContainer de SwiftData,
setup de Environment, AppDelegate adapters si hicieran falta.
